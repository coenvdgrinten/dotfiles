# Safe monitor defaults.
# Keep the internal panel enabled so Hyprland always has an active output.
# External outputs will auto-enable when connected.
monitor=eDP-1,preferred,auto,1
monitor=HDMI-A-1,preferred,auto,1
