# Cross-machine monitor defaults.
# Use connector-agnostic rules so different laptop output names still work.
# Per-machine overrides can still be applied by nwg-displays in this same file.
monitor=,preferred,auto,1
