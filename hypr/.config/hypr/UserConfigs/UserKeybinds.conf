# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# This is where you put your own keybinds. Be Mindful to check as well ~/.config/hypr/configs/Keybinds.conf to avoid conflict
# if you think I should replace the Pre-defined Keybinds in ~/.config/hypr/configs/Keybinds.conf , submit an issue or let me know in DC and present me a valid reason as to why, such as conflicting with global shortcuts, etc etc

# See https://wiki.hyprland.org/Configuring/Keywords/ for more settings and variables
# See also Laptops.conf for laptops keybinds 

# /* ---- ✴️ Variables ✴️ ---- */  #
$mainMod = SUPER
$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts
$UserConfigs = $HOME/.config/hypr/UserConfigs

#  IMPORTANT: If you want to remap and existing keybind you MUST unbindd it first 

# The bindings are CASE SENSITIVE. We suggest you copy the exisitng binding here
#  Then change `bindd` to `unbind`

# E.g. 
# unbind = $mainMod, Return, Open terminal, exec, $term
# bindd = $mainMod, Return, Open terminal, exec, ghostty 
#
# unbind = $mainMod, E, file manager, exec, $files
# bindd = $mainMod, T, file manager, exec, $files

# If you are ADDING a bindd, make sure you include the description 
# Other the keybind search menu might not show it properly 

# E.g.
# bindd = $mainMod, Z, My z app, exec APPNAME

# Pixel Buds audio profile toggle
bindd = $mainMod, F9, switch Pixel Buds to high-quality audio, exec, ~/.local/bin/pixel-buds-a2dp.sh a2dp
bindd = $mainMod SHIFT, F9, switch Pixel Buds to hands-free (mic), exec, ~/.local/bin/pixel-buds-a2dp.sh hfp

# Session management
bindd = $mainMod CTRL, S, save current workspace session, exec, $scriptsDir/save-session.sh
bindd = $mainMod CTRL, R, restore saved workspace session, exec, $scriptsDir/restore-session.sh

# Window switcher with rofi
bindd = $mainMod CTRL, W, window switcher with thumbnails, exec, $scriptsDir/rofi-window-switcher.sh

# Scratchpad terminal (drops down on toggle)
bindd = $mainMod, Z, toggle scratchpad terminal, exec, $scriptsDir/scratchpad.sh

# Auto monitor profile (manually trigger)
bindd = $mainMod ALT, M, apply auto monitor profile, exec, $scriptsDir/auto-monitor-profile.sh

# For passthrough keyboard into a VM
# bind = $mainMod ALT, P, submap, passthru
#submap = passthru
# to unbind
#bind = $mainMod ALT, P, submap, reset
#submap = reset

# ---------------------------------------------------------
# Override default workspace switches to ALWAYS pull the
# workspace to the current monitor
# ---------------------------------------------------------
unbind = $mainMod, code:10
unbind = $mainMod, code:11
unbind = $mainMod, code:12
unbind = $mainMod, code:13
unbind = $mainMod, code:14
unbind = $mainMod, code:15
unbind = $mainMod, code:16
unbind = $mainMod, code:17
unbind = $mainMod, code:18
unbind = $mainMod, code:19

bindd = $mainMod, code:10, workspace 1, exec, ~/.config/hypr/scripts/workspace-action.sh 1
bindd = $mainMod, code:11, workspace 2, exec, ~/.config/hypr/scripts/workspace-action.sh 2
bindd = $mainMod, code:12, workspace 3, exec, ~/.config/hypr/scripts/workspace-action.sh 3
bindd = $mainMod, code:13, workspace 4, exec, ~/.config/hypr/scripts/workspace-action.sh 4
bindd = $mainMod, code:14, workspace 5, exec, ~/.config/hypr/scripts/workspace-action.sh 5
bindd = $mainMod, code:15, workspace 6, exec, ~/.config/hypr/scripts/workspace-action.sh 6
bindd = $mainMod, code:16, workspace 7, exec, ~/.config/hypr/scripts/workspace-action.sh 7
bindd = $mainMod, code:17, workspace 8, exec, ~/.config/hypr/scripts/workspace-action.sh 8
bindd = $mainMod, code:18, workspace 9, exec, ~/.config/hypr/scripts/workspace-action.sh 9
bindd = $mainMod, code:19, workspace 10, exec, ~/.config/hypr/scripts/workspace-action.sh 10
