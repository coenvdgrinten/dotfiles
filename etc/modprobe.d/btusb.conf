# Disable USB autosuspend for Bluetooth adapter
# This prevents the kernel from putting the Bluetooth USB adapter to sleep,
# which is a common cause of Pixel Buds disconnect/reconnect loops
options btusb enable_autosuspend=n
